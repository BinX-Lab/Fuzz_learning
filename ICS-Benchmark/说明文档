这是设备仿真模块
仿真的目录规范是：厂商-型号-每个cve的专属环境






  一个设备启动需要的文件：
    #1.env文件 --- 写入docker容器环境变量，可在容器中全局调用、环境变量（针对这个漏洞环境，容器内qemu仿真所需要的启动参数如ip、端口）等文字记录
    #2.yml文件 --- 
    （1）benchmark.yml文件记录了启动该环境容器的实际信息(如ip和端口)、攻击依赖（如是否需要验证）
    （2）dev——build 记录怎么启动
    #3.dockerfile文件 --- 记录docker容器更加具体的启动操作（实际怎么启动qemu）
    #4.sh
    （1）run.sh文件 --- 记录了docekr容器中，qemu里的实际执行脚本
    （2）tool.sh文件 --- 该漏洞需要的环境（攻击所需要的工具pwntools等）
    #5.firmware文件 --- 记录了固件文件

